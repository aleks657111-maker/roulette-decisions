- generic [active] [ref=f4e1]: ":root { --bg: #ffffff; --panel: #f4f5f9; --text: #23252f; --muted: #74778a; --accent: #6c5ce7; --accent-hover: #7d6ef0; --danger: #e74c3c; --border: #e5e6ee; } * { box-sizing: border-box; } html { overflow-x: hidden; } body { margin: 0; min-height: 100vh; background: var(--bg); color: var(--text); font-family: \"Segoe UI\", system-ui, sans-serif; display: flex; flex-direction: column; align-items: center; padding: 24px; overflow-x: hidden; } h1 { margin: 0 0 24px; font-size: 1.8rem; } .app { display: flex; flex-wrap: wrap; gap: 40px; justify-content: center; align-items: flex-start; width: 100%; max-width: 900px; } .wheel-area { display: flex; flex-direction: column; align-items: center; gap: 16px; } .wheel-wrapper { position: relative; width: 360px; max-width: 82vw; aspect-ratio: 1 / 1; display: flex; align-items: center; justify-content: center; } .wheel-ring { position: absolute; inset: -8px; border-radius: 50%; background: conic-gradient( from 0deg, #ff3b3b, #ff9f1a, #ffe14d, #63e06b, #29c5d6, #4d7bff, #a855f7, #ff3b3b ); filter: blur(0.2px); z-index: 0; } #wheel { position: relative; width: 100%; height: 100%; border-radius: 50%; box-shadow: 0 0 0 6px #ffffff, 0 10px 28px rgba(30, 20, 60, 0.18); will-change: transform; z-index: 1; } .pointer { position: absolute; top: -6px; left: 50%; transform: translateX(-50%); width: 0; height: 0; border-left: 16px solid transparent; border-right: 16px solid transparent; border-top: 28px solid #ff5252; z-index: 6; filter: drop-shadow(0 2px 3px rgba(0, 0, 0, 0.25)); } .spin-btn { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 78px; height: 78px; padding: 0; font-size: 1rem; font-weight: 700; line-height: 1.1; border: none; border-radius: 50%; background: var(--accent); color: #fff; cursor: pointer; box-shadow: 0 0 0 5px #ffffff, 0 0 0 7px var(--border), 0 6px 14px rgba(30, 20, 60, 0.3); transition: background 0.15s ease, transform 0.1s ease; z-index: 5; } .spin-btn:hover:not(:disabled) { background: var(--accent-hover); } .spin-btn:active:not(:disabled) { transform: translate(-50%, -50%) scale(0.96); } .spin-btn:disabled { opacity: 0.5; cursor: not-allowed; } .hint { color: var(--danger); font-size: 0.9rem; margin: 0; } .items-area { flex: 1; min-width: 260px; max-width: 360px; background: var(--panel); border: 1px solid var(--border); border-radius: 12px; padding: 20px; } .items-area h2 { margin: 0 0 12px; font-size: 1.1rem; } .items-list { list-style: none; margin: 0 0 16px; padding: 0; display: flex; flex-direction: column; gap: 8px; max-height: 320px; overflow-y: auto; } .item-row { display: flex; align-items: center; gap: 8px; background: #ffffff; border: 1px solid var(--border); padding: 8px 10px; border-radius: 8px; } .item-color { width: 12px; height: 12px; border-radius: 50%; flex-shrink: 0; } .item-label { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; } .item-input { flex: 1; padding: 4px 6px; border-radius: 6px; border: 1px solid var(--accent); background: var(--bg); color: var(--text); font-size: 0.95rem; } .icon-btn { border: none; background: transparent; color: var(--muted); cursor: pointer; font-size: 1rem; padding: 4px 6px; border-radius: 6px; line-height: 1; } .icon-btn:hover { background: var(--panel); color: var(--text); } .icon-btn.delete:hover { color: var(--danger); } .add-form { display: flex; gap: 8px; } .add-form input { flex: 1; padding: 8px 10px; border-radius: 8px; border: 1px solid var(--border); background: #ffffff; color: var(--text); font-size: 0.95rem; } .add-form button { padding: 8px 14px; border: none; border-radius: 8px; background: var(--accent); color: #fff; cursor: pointer; font-weight: 600; } .add-form button:hover { background: var(--accent-hover); } .result-overlay { position: fixed; inset: 0; background: rgba(0, 0, 0, 0.75); display: flex; align-items: center; justify-content: center; z-index: 100; animation: fadeIn 0.2s ease; } .result-overlay[hidden] { display: none; } .result-box { background: #ffffff; padding: 48px 56px; border-radius: 20px; text-align: center; max-width: 90vw; box-shadow: 0 20px 50px rgba(30, 20, 60, 0.35); animation: popIn 0.25s ease; } .result-label { margin: 0; color: var(--muted); font-size: 1.1rem; } .result-text { margin: 8px 0 32px; font-size: clamp(1.8rem, 6vw, 3.2rem); color: var(--accent-hover); word-break: break-word; } .result-buttons { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; } .result-buttons button { padding: 12px 24px; border: none; border-radius: 999px; font-size: 1rem; font-weight: 600; cursor: pointer; } #spinAgainBtn { background: var(--accent); color: #fff; } #spinAgainBtn:hover { background: var(--accent-hover); } #closeResultBtn { background: var(--panel); color: var(--text); border: 1px solid var(--border); } #closeResultBtn:hover { background: var(--border); } @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } } @keyframes popIn { from { transform: scale(0.85); opacity: 0; } to { transform: scale(1); opacity: 1; } } .confetti-layer { position: fixed; inset: 0; pointer-events: none; z-index: 200; overflow: hidden; } .confetti-piece { position: absolute; top: -20px; width: 8px; height: 14px; opacity: 0.95; animation-name: confetti-fall; animation-timing-function: linear; animation-fill-mode: forwards; } @media (max-width: 600px) { body { padding: 16px; } h1 { font-size: 1.4rem; margin-bottom: 16px; text-align: center; } .app { gap: 24px; } .wheel-wrapper { width: 300px; max-width: 78vw; } .spin-btn { width: 64px; height: 64px; font-size: 0.85rem; } .items-area { width: 100%; max-width: 420px; } .result-box { padding: 32px 24px; } .result-buttons button { padding: 10px 18px; font-size: 0.95rem; } } @keyframes confetti-fall { 0% { transform: translateY(0) translateX(0) rotate(0deg); opacity: 1; } 85% { opacity: 1; } 100% { transform: translateY(var(--fall-distance)) translateX(var(--drift)) rotate(var(--spin)); opacity: 0; } }"